// src/components/shared/MessageCard.tsx
// Card component for displaying individual messages in the output stream

import React from 'react';

interface MessageCardProps {
  // Props will be defined during implementation
}

export const MessageCard: React.FC<MessageCardProps> = (props) => {
  return (
    <div>
      {/* Message card content */}
    </div>
  );
};
